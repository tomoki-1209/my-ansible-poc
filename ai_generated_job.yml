---
- name: Check current users
  hosts: all
  gather_facts: no
  tasks:
    - name: Get list of users
      getent:
        database: passwd
      register: user_list

    - name: Display current users
      debug:
        msg: "{{ user_list.ansible_facts.getent_passwd.keys() | list }}"