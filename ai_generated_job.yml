---
- name: Get current IAM user information
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Get current AWS identity
      amazon.aws.aws_caller_info:
      register: caller_info

    - name: Display IAM user information
      debug:
        msg: "Current IAM User: {{ caller_info.iam_user_arn }}"